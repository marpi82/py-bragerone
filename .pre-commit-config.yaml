minimum_pre_commit_version: "4.3.0"

exclude: |
  (?x)^(
    src/docs/_build/|
    dist/|
    build/|
    .*/migrations/|
    .*/node_modules/
  )

repos:
  # Ruff → linter + formatter
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.1
    hooks:
      - id: ruff
        args: ["--fix"]
      - id: ruff-format

  # Mypy → type checking
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.18.2
    hooks:
      - id: mypy
        name: mypy
        entry: uv run mypy
        language: system
        types: [python]
        files: ^(src/|tests/)
        pass_filenames: false

  # Secrets scanning with Gitleaks
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks
        name: gitleaks (protect staged)
        entry: gitleaks protect --staged --redact
        language: golang
        pass_filenames: false

  # Useful generic checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=2048"]
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: trailing-whitespace

  # --- SECURITY: code (SAST) ---
  - repo: https://github.com/PyCQA/bandit
    rev: 1.8.6
    hooks:
      - id: bandit
        name: bandit (scan src/)
        args: ["-r", "src", "-q"]
        pass_filenames: false

  # --- SECURITY: Semgrep ---
  - repo: https://github.com/returntocorp/semgrep
    rev: v1.89.0
    hooks:
      - id: semgrep
        name: semgrep (p/ci rules)
        args: ["--config", "p/ci", "--error"]
        pass_filenames: true

  # --- SECURITY: dependencies (SCA) with uv export + pip-audit ---
  # NOTE: Disabled for faster commits. Runs automatically in CI with GitHub Code Scanning
  # Strategy: Generate requirements snapshot on-the-fly during pre-commit, then audit it
  # - repo: local
  #   hooks:
  #     - id: uv-export
  #       name: uv pip freeze requirements snapshot
  #       language: system
  #       entry: bash -c 'uv pip freeze --exclude-editable > requirements-pip-audit.txt'
  #       pass_filenames: false
  #       stages: [pre-commit]
  #       always_run: true # Run even if uv.lock unchanged

  # - repo: https://github.com/pypa/pip-audit
  #   rev: v2.9.0
  #   hooks:
  #     - id: pip-audit
  #       name: pip-audit (scan requirements snapshot)
  #       args:
  #         [
  #           "--requirement",
  #           "requirements-pip-audit.txt",
  #           "--progress-spinner",
  #           "off",
  #         ]
  #       stages: [pre-commit]
  #       always_run: true # Run even if requirements.txt not staged

  # --- TESTY na pre-push z progiem coverage ---
  - repo: local
    hooks:
      - id: pytest-pre-push
        name: pytest (coverage gate on push)
        language: system
        entry: bash -lc 'uv run --group test pytest --maxfail=1 --disable-warnings -q --cov=src --cov-report=term-missing --cov-fail-under=80'
        pass_filenames: false
        stages: [pre-push]
